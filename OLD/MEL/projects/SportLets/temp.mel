int $start = 1;
int $frame = frame;

seed(1);
int $light = rand(1.1,2.9);
int $open = rand(4,8.99);
int $close = rand(2,4.99);

if($light == 1)
{
	sets -e -fe surfaceShader2SG s003001IntSpaceShipBridge_c_hi_light_10_2;
	if($frame >= ($start+$open))
	{
		
	}
}
else
	sets -e -fe surfaceShader1SG s003001IntSpaceShipBridge_c_hi_light_10_2;