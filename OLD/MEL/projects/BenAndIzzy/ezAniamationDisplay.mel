global proc ezAniamationDisplay()
{
	string $version = "7.0";
	string $source = substituteAllString(`getenv "DEV_PATH"`, "/", "\\") + "\\Maya\\" + $version + "\\prefs\\markingMenus";
	slUpdataCustomeFiles ($source, $version);

	print "OK!";
}