// createPointCacheAssetPreCallback.mel
/*
	Per project (for all assets) callback to call when createPointCacheAsset is called, but before anything is done.
	This is called before the per-asset callback (pointCacheAssetCreationPreCallback.mel) is called, if one exists.
*/

// ------------------------------------------------------------------------------------------------
global proc createPointCacheAssetPreCallback(string $charGrp)
{
	
	
}


